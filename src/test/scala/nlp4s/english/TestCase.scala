package nlp4s.english

case class TestCase(
  text: String,
  parses: Int,
  tokens: Int, // does not include "wall" token but includes concatenated tokens
  ignore: Boolean,
  )

object TestCase {
  val all: List[TestCase] =
    List(
      TestCase("move", 1, 1, false),
      TestCase("the man runs", 1, 3, false),
      TestCase("the man sees the table", 1, 5, false),
      TestCase("the man picked up the table", 1, 5, false),
      TestCase("pick up the table", 1, 3, false),
      TestCase("pick up the mans leg", 1, 4, false),
      TestCase("he drinks his beer", 1, 4, false),
      TestCase("take the mans beer", 1, 4, false),
      TestCase("the beer is mine", 1, 4, false),
      TestCase("I move", 1, 2, false),
      TestCase("he ran", 1, 2, false),
      TestCase("they are looking", 1, 3, false),
      TestCase("it is a table", 1, 4, false),
      TestCase("she is walking", 1, 3, false),
      TestCase("you pick up the table", 2, 4, false),
      TestCase("he picks up my table", 1, 4, false),
      TestCase("i pick up the table", 1, 4, false),
      TestCase("tomorrow i pick up the table", 1, 5, false),
      TestCase("i pick up the table tomorrow", 1, 5, false),
      TestCase("she picked up the table", 1, 4, false),
      TestCase("we are picking up the table", 1, 5, false),
      TestCase("i am drinking", 1, 3, false),
      TestCase("I am drinking the beer", 1, 5, false),
      TestCase("I see the running man", 1, 5, false),
      TestCase("I see the man drinking the beer", 1, 7, false),
      TestCase("I see the men running", 1, 5, false),
      TestCase("i was drinking", 1, 3, false),
      TestCase("I drank it myself", 1, 4, false),
      TestCase("he is himself", 1, 3, false),
      TestCase("they pick up themselves", 1, 3, false),
      TestCase("he run ran", 0, 3, false),
      TestCase("he she it runs", 0, 4, false),
      TestCase("we pick up the women", 1, 4, false),
      TestCase("he will run to the table", 1, 6, false),
      TestCase("he will be running", 1, 4, false),
      TestCase("will he be taking the table", 1, 6, false),
      TestCase("will he run to the table", 1, 6, false),
      TestCase("he will ran to the table", 0, 6, false),
      TestCase("he will runs to the table", 0, 6, false),
      TestCase("he will running to the table", 0, 6, false),
      TestCase("I am running", 1, 3, false),
      TestCase("he is running to the table", 1, 6, false),
      TestCase("is he running to the table", 1, 6, false),
      TestCase("the women drank", 1, 3, false),
      TestCase("the women drinks", 0, 3, false),
      TestCase("the quick man drinks the slow drink", 1, 7, false),
      TestCase("the quick helpful man drinks the slow drink", 1, 8, false),
      TestCase("the men walks over the road", 0, 6, false),
      TestCase("the men run to the house", 1, 6, false),
      TestCase("quickly drink the drink", 1, 4, false),
      TestCase("drink the drink quickly", 1, 4, false),
      TestCase("you picks up the table", 0, 4, false),
      TestCase("who picks up the table", 1, 4, false),
      TestCase("who is picking up the table", 1, 5, false),
      TestCase("who pick up the table", 0, 4, false),
      TestCase("who runs", 1, 2, false),
      TestCase("who runs quickly", 1, 3, false),
      TestCase("what ran to the house", 1, 5, false),
      TestCase("who ran", 1, 2, false),
      TestCase("who is running", 1, 3, false),
      TestCase("who was running", 1, 3, false),
      TestCase("who was not running to the house", 1, 7, false),
      TestCase("who were not running", 0, 4, false),
      TestCase("who run", 0, 2, false),
      TestCase("are they a table", 1, 4, false),
      )
}


